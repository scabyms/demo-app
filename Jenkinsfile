pipeline {
  stages {
    stage('Static Analysis') {
      steps {
         echo 'Static Analysis'
         sh 'ls'
       }
    },
    stage('Unit Test') {
      steps {
        echo "help"
        sh 'ps'
      }
    }
  }
}
